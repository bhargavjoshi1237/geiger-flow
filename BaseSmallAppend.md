Do not implement this as a basic placeholder.

Before coding, inspect the related files, folders, components, routes, UI patterns, forms, dialogs, actions, schemas, and data flow. Understand how similar features are already built in this Next.js 16 App Router project.

Implement the feature as a complete, realistic, production-quality part of the app. Match the existing visual style, shadcn/ui usage, Tailwind patterns, lucide icons, file structure, naming conventions, server/client component boundaries, and interaction flows.

If similar create/edit/delete actions use dialogs, sheets, forms, validation, toast messages, confirmation modals, loading states, or empty states, follow the same pattern. Do not add buttons or UI elements that do nothing. Do not add placeholder-only screens. Include realistic fields, actions, states, and flows based on the domain and existing app context.

Do not leave comments, TODOs, console logs, unused imports, dead code, or fake placeholder logic. Review the final implementation for consistency and completeness.