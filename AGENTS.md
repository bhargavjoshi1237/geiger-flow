@MODULE_CONVENTIONS.md

# Agent Guidelines

Follow the module conventions above when building or changing project features
(database, data layer, permissions, UI). Reuse existing files, patterns, naming,
folder structure, and the semantic color tokens rather than introducing new ones.
