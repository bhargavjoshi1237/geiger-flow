"use client";

// Canonical implementation now lives in @geiger/ui (absorbed from this fork).
// Kept as a re-export so existing `@/components/internal/shared/segmented_tabs`
// imports and the .design-sync mapping keep resolving to the package version.
export { SegmentedTabs } from "@geiger/ui";
